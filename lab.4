file_path = input("Введіть шлях до файлу: ") # Запит шляху до файлу від користувача
if not os.path.exists(file_path): print # Перевірка чи файл існує
lines_count = 0 empty_lines_count = 0 z_lines_count = 0 z_count = 0 and_lines_count = 0  # Ініціалізація змінних для статистики 
 with open(file_path, 'r') as f: for line in f: lines_count += 1 # Читання файлу по рядках
if line.strip() == '': empty_lines_count += 1 # Перевірка на порожній рядок
"z" у рядку if 'z' in line: z_lines_count += 1 # Перевірка на наявність літери
"z" у файлі z_count += line.count('z') # Підрахунок кількості літер
line: and_lines_count += 1 # Перевірка на наявність групи символів
 another_file = input("Аналізувати ще один файл? (так/ні): ") if another_file.lower() != 'так': breakу another_file = input("Аналізувати ще один файл? (так/ні): ") if another_file.lower() != 'так': break  # Запит на аналіз іншого файлу another_file = input("Аналізувати ще один файл? (так/ні): ") if another_file.lower() != 'так': break
